import React, { Component } from 'react';
import './GifCard.css'

class GifCard extends Component {
    constructor(props){
        super(props);
    }
    render() {
        return
    }
}

export default GifCard;